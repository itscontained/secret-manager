apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secret-manager.fullname" . }}-test-connection"
  labels:
    {{- include "secret-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secret-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
