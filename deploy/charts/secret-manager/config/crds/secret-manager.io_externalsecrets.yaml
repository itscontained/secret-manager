
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: externalsecrets.secret-manager.io
spec:
  additionalPrinterColumns:
  - JSONPath: .status.conditions[?(@.type=='Ready')].status
    name: READY
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: AGE
    type: date
  group: secret-manager.io
  names:
    categories:
    - secretmanager
    kind: ExternalSecret
    listKind: ExternalSecretList
    plural: externalsecrets
    singular: externalsecret
  preserveUnknownFields: false
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: ExternalSecret is the Schema for the externalsecrets API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ExternalSecretSpec defines the desired state of ExternalSecret
          properties:
            data:
              description: Data is a list of references to secrets values
              items:
                properties:
                  remoteRef:
                    description: RemoteRef describes the path and other parameters
                      to access the secret for the specific SecretManager
                    properties:
                      key:
                        description: Key to extract secret value at path in SecretManager
                          if path specifies multiple secret values. Can be omitted
                          if not supported by SecretManager or if entire secret should
                          be fetched.
                        type: string
                      path:
                        description: Path to the key in the SecretManager
                        type: string
                      version:
                        description: Version of the secret to fetch from the SecretManager.
                        type: string
                    required:
                    - path
                    type: object
                  secretKey:
                    description: The key in the generated secret to place fetched
                      value into. If not specified and the SecretManager value has
                      multiple keys, all keys will be placed into secret with the
                      key name from the source. If omitted and only a single value
                      is present in SecretManager value, the data will be placed under
                      the 'secret' key in the generated secret.
                    type: string
                required:
                - remoteRef
                type: object
              type: array
            managerRef:
              description: ManagerRef is a reference to the manager backend for this
                secret. If the 'kind' field is not set, or set to 'SecretManager',
                a SecretManager resource with the given name in the same namespace
                as the SecretManager will be used. If the 'kind' field is set to 'ClusterSecretManager',
                a ClusterSecretManager with the provided name will be used. The 'name'
                field in this stanza is required at all times.
              properties:
                group:
                  description: Group of the resource being referred to.
                  type: string
                kind:
                  description: Kind of the resource being referred to.
                  type: string
                name:
                  description: Name of the resource being referred to.
                  type: string
              required:
              - name
              type: object
            renewAfter:
              description: The amount of time before the secret-manager will renew
                the values of of the ExternalSecret. If not set the secret will only
                be synced on creation of the ExternalSecret.
              format: duration
              type: string
            template:
              description: Template which will be deep merged into the generated secret.
                Can be used to set for example annotations or type on the generated
                secret.
              format: any
              type: object
          required:
          - data
          - managerRef
          type: object
        status:
          description: ExternalSecretStatus defines the observed state of ExternalSecret
          properties:
            conditions:
              description: Conditions of the resource.
              items:
                description: A Condition that may apply to a resource.
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime is the last time this condition
                      transitioned from one status to another.
                    format: date-time
                    type: string
                  message:
                    description: A Message containing details about this condition's
                      last transition from one status to another, if any.
                    type: string
                  reason:
                    description: A Reason for this condition's last transition from
                      one status to another.
                    type: string
                  status:
                    description: Status of this condition; is it currently True, False,
                      or Unknown?
                    type: string
                  type:
                    description: Type of this condition. At most one of each condition
                      type may apply to a resource at any point in time.
                    type: string
                required:
                - lastTransitionTime
                - reason
                - status
                - type
                type: object
              type: array
            renewalTime:
              description: RenewalTime is the time at which the secret values will
                be next renewed. If not set, no upcoming renewal is scheduled.
              format: date-time
              type: string
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
